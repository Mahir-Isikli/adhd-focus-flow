"use client"; // Add this line at the top of the file

import { Component as ADHDTaskTracker } from "@/components/component";

export default function Home() {
  return (
    <main className="min-h-screen">
      <ADHDTaskTracker />
    </main>
  );
}
